{
  "name": "dashboard-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run start-front\" \"npm run start-back\"",
    "start-front": "cd Front_end && npm run dev",
    "start-back": "cd Back_end && streamlit run dashboard_app.py --server.headless true --server.port 8501"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
